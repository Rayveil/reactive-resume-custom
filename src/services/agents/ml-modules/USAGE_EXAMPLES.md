# ML Modules - Usage Examples & Integration Patterns\n\n## Table of Contents\n\n1. [Basic Examples](#basic-examples)\n2. [HRAgent Integration](#hragent-integration)\n3. [Advanced Patterns](#advanced-patterns)\n4. [Error Handling](#error-handling)\n\n---\n\n## Basic Examples\n\n### Example 1: Quick Resume Screening\n\n**Use case:** Screen 100+ resumes for a single job posting\n\n``typescript\nimport { quickAnalyze } from '@/services/agents/ml-modules';\n\nconst jobInput = {\n  title: \"Senior Backend Developer\",\n  company: \"TechCorp\",\n  requiredSkills: [\"Python\", \"Docker\", \"Kubernetes\", \"AWS\", \"PostgreSQL\"],\n  requiredEducation: \"bachelor\",\n  location: \"San Francisco\",\n};\n\nconst resumes = [...]; // Array of ResumeData objects\nconst screeningResults = [];\n\nfor (const resume of resumes) {\n  const analysis = await quickAnalyze(resume, jobInput);\n  \n  screeningResults.push({\n    candidateName: resume.basics?.name,\n    matchScore: analysis.scoring.finalScore,\n    tier: analysis.scoring.matchTier,\n    missingSkills: analysis.skillGap.missingSkills,\n    topIssues: analysis.improvements.priorities.slice(0, 2),\n  });\n}\n\n// Sort by match score\nscreeningResults.sort((a, b) => b.matchScore - a.matchScore);\n\n// Show top 10 candidates\nconst topCandidates = screeningResults.slice(0, 10);\ntopCandidates.forEach((c) => {\n  console.log(`${c.candidateName}: ${c.matchScore}/100 (${c.tier})`);\n});\n``\n\n**Output:**\n`\nAlice Chen: 92.3/100 (excellent)\nBob Martinez: 85.1/100 (good)\nCharlie Wong: 78.5/100 (qualified)\nDiana Patel: 68.2/100 (qualified)\nEric Johnson: 45.0/100 (unqualified)\n`\n\n---\n\n### Example 2: Detailed Candidate Analysis\n\n**Use case:** Generate detailed feedback for a single candidate\n\n``typescript\nimport { MLModulesManager } from '@/services/agents/ml-modules';\n\nconst manager = new MLModulesManager();\n\nconst resume = { /* ResumeData */ };\nconst jobInput = { /* Job description */ };\n\nconst analysis = await manager.analyzeMatch(resume, jobInput, useLLM: false);\n\n// Generate detailed report\nconst report = `\n## Resume Match Analysis for ${resume.basics?.name}\n\n### Overall Match: ${analysis.scoring.finalScore}/100 (${analysis.scoring.matchTier})\n\n### Match Breakdown:\n- Semantic Alignment: ${analysis.semantic.semanticScore}/100\n- Skill Coverage: ${analysis.skillGap.skillCoverage}%\n- Formula: ${analysis.matchScoreBreakdown.formula}\n\n### Skills Analysis:\nMatched Skills: ${analysis.skillGap.matchedSkills.join(\", \") || \"None\"}\nMissing Skills: ${analysis.skillGap.missingSkills.join(\", \")}\nBonus Skills: ${analysis.skillGap.bonusSkills.join(\", \") || \"None\"}\n\n### Top Improvement Areas:\n${analysis.improvements.priorities\n  .map(\n    (rec, idx) => `\n${idx + 1}. ${rec.suggestion} (${rec.priority})\n   How: ${rec.implementation}\n   Impact: ${rec.impact}\n  `\n  )\n  .join(\"\\n\")}\n\n### Recommended Next Steps:\n${analysis.improvements.summary}\nEstimated score improvement: +${analysis.improvements.estimatedScoreIncrease} points\n`;\n\nconsole.log(report);\n``\n\n**Output:**\n`\n## Resume Match Analysis for Alice Chen\n\n### Overall Match: 92.3/100 (excellent)\n\n### Match Breakdown:\n- Semantic Alignment: 88/100\n- Skill Coverage: 100%\n- Formula: (88 × 0.7) + (100 × 0.3) = 92.3\n\n### Skills Analysis:\nMatched Skills: Python, Docker, Kubernetes, AWS, PostgreSQL\nMissing Skills: None\nBonus Skills: Terraform, Datadog\n\n### Top Improvement Areas:\n1. Highlight your extra skills to differentiate from other candidates (high)\n   How: Create a \"Certifications & Additional Skills\" section...\n   Impact: +5 points (competitive advantage)\n`\n\n---\n\n### Example 3: Custom Scoring Configuration\n\n**Use case:** Adjust weights for different job types\n\n``typescript\nimport { MLModulesManager } from '@/services/agents/ml-modules';\n\n// For technical roles: Emphasize skill match\nconst techManager = new MLModulesManager({\n  semanticWeight: 0.5,  // 50% semantic\n  skillWeight: 0.5,     // 50% skills\n});\n\n// For leadership roles: Emphasize semantic alignment (background/experience)\nconst leadershipManager = new MLModulesManager({\n  semanticWeight: 0.8,  // 80% semantic\n  skillWeight: 0.2,     // 20% skills\n});\n\n// Analyze with different weights\nconst techAnalysis = await techManager.analyzeMatch(resume, techJob);\nconst leadershipAnalysis = await leadershipManager.analyzeMatch(\n  resume,\n  leadershipJob\n);\n\nconsole.log(`Tech role match: ${techAnalysis.scoring.finalScore}`);\nconsole.log(`Leadership role match: ${leadershipAnalysis.scoring.finalScore}`);\n``\n\n---\n\n## HRAgent Integration\n\n### Integration Pattern 1: Enhance HRAssessmentResult\n\n**Goal:** Add ML module insights to existing HRAgent output\n\n``typescript\nimport { HRAgent } from '@/services/agents/hr-agent-enhanced';\nimport { MLModulesManager } from '@/services/agents/ml-modules';\nimport type { HRAssessmentResult } from '@/services/agents/hr-agent-enhanced';\n\nclass EnhancedHRAgent extends HRAgent {\n  private mlManager: MLModulesManager;\n\n  constructor() {\n    super();\n    this.mlManager = new MLModulesManager();\n  }\n\n  async evaluateResume(\n    jdInput: any,\n    memoryId: string\n  ): Promise<HRAssessmentResult & { mlAnalysis: any }> {\n    // Get base HRAgent result\n    const baseResult = await super.evaluateResume(jdInput, memoryId);\n\n    // Get resume from memory\n    const allResumes = await getResumeFromMemory();\n    const resumeEntry = allResumes.find((r) => r.memoryId === memoryId);\n\n    if (!resumeEntry) {\n      throw new Error(`Resume ${memoryId} not found`);\n    }\n\n    // Run ML analysis\n    const mlAnalysis = await this.mlManager.analyzeMatch(\n      resumeEntry.data,\n      jdInput,\n      true // Use LLM for recommendations\n    );\n\n    // Combine results\n    return {\n      ...baseResult,\n      mlAnalysis: {\n        semanticScore: mlAnalysis.semantic.semanticScore,\n        skillCoverage: mlAnalysis.skillGap.skillCoverage,\n        matchFormula: mlAnalysis.matchScoreBreakdown.formula,\n        improvements: mlAnalysis.improvements,\n        skillGapDetails: {\n          matched: mlAnalysis.skillGap.matchedSkills,\n          missing: mlAnalysis.skillGap.missingSkills,\n          bonus: mlAnalysis.skillGap.bonusSkills,\n        },\n      },\n    };\n  }\n}\n\n// Usage\nconst enhancedHR = new EnhancedHRAgent();\nconst result = await enhancedHR.evaluateResume(jdInput, memoryId);\n\nconsole.log(result.mlAnalysis.improvements.recommendations);\n``\n\n---\n\n### Integration Pattern 2: ML-First Assessment\n\n**Goal:** Use ML modules as primary assessment, HR dimensions as secondary\n\n``typescript\nimport { MLModulesManager, type CompleteMLAnalysis } from '@/services/agents/ml-modules';\n\nclass MLFirstHRAgent {\n  private mlManager: MLModulesManager;\n\n  constructor() {\n    this.mlManager = new MLModulesManager({\n      semanticWeight: 0.7,\n      skillWeight: 0.3,\n    });\n  }\n\n  async assessResume(resume: ResumeData, jobInput: any): Promise<AssessmentReport> {\n    // Primary: ML analysis\n    const mlAnalysis = await this.mlManager.analyzeMatch(resume, jobInput, true);\n\n    // Filter by ML score\n    if (mlAnalysis.scoring.finalScore < 60) {\n      return {\n        status: \"not_qualified\",\n        reason: `ML score ${mlAnalysis.scoring.finalScore} below threshold`,\n        mlScore: mlAnalysis.scoring.finalScore,\n        recommendations: [],\n      };\n    }\n\n    // Secondary: HR dimensions for qualified candidates\n    const hrAssessment = await this.assessHRDimensions(resume, jobInput);\n\n    return {\n      status: \"qualified\",\n      mlScore: mlAnalysis.scoring.finalScore,\n      hrScore: hrAssessment.score,\n      recommendations: this.combineRecommendations(\n        mlAnalysis.improvements.recommendations,\n        hrAssessment.suggestions\n      ),\n    };\n  }\n\n  private async assessHRDimensions(resume: ResumeData, jobInput: any) {\n    // Implement HR dimension assessment (6 dimensions from HRAgent)\n    // Only run for candidates who pass ML screening\n    return { score: 75, suggestions: [] };\n  }\n\n  private combineRecommendations(mlRecs: any[], hrSuggestions: any[]) {\n    return [\n      ...mlRecs.slice(0, 3), // Top 3 ML recommendations\n      ...hrSuggestions.slice(0, 2), // Top 2 HR suggestions\n    ];\n  }\n}\n``\n\n---\n\n## Advanced Patterns\n\n### Pattern 1: Batch Processing with Progress\n\n**Use case:** Process 1000+ resumes with progress tracking\n\n``typescript\nimport { quickAnalyze } from '@/services/agents/ml-modules';\nimport pMap from 'p-map';  // npm install p-map\n\nclass BatchProcessor {\n  async analyzeResumes(\n    resumes: ResumeData[],\n    jobInput: any,\n    concurrency = 5,\n    onProgress?: (current: number, total: number) => void\n  ) {\n    const results = await pMap(\n      resumes,\n      async (resume, idx) => {\n        const analysis = await quickAnalyze(resume, jobInput);\n        onProgress?.(idx + 1, resumes.length);\n        return {\n          resume: resume.basics?.name,\n          score: analysis.scoring.finalScore,\n          tier: analysis.scoring.matchTier,\n        };\n      },\n      { concurrency }\n    );\n\n    return results.sort((a, b) => b.score - a.score);\n  }\n}\n\n// Usage\nconst processor = new BatchProcessor();\nconst results = await processor.analyzeResumes(\n  resumes,\n  jobInput,\n  10,  // Process 10 at a time\n  (current, total) => {\n    console.log(`Progress: ${current}/${total}`);\n  }\n);\n``\n\n---\n\n### Pattern 2: Skill Development Path Generation\n\n**Use case:** Suggest learning path to bridge skill gaps\n\n``typescript\nimport { analyzeSkillGap } from '@/services/agents/ml-modules';\n\nclass LearningPathGenerator {\n  private skillResources = {\n    Python: [\n      { name: 'Python for Data Analysis', source: 'Coursera', hours: 40 },\n      { name: 'Advanced Python', source: 'Udemy', hours: 30 },\n    ],\n    Docker: [\n      { name: 'Docker Deep Dive', source: 'Linux Academy', hours: 20 },\n    ],\n    // ... more skills\n  };\n\n  generateLearningPath(\n    currentSkills: string[],\n    targetSkills: string[],\n    priority = 'balanced' // 'quick', 'comprehensive', 'balanced'\n  ) {\n    const gaps = analyzeSkillGap(targetSkills, currentSkills);\n\n    const path = gaps.missingSkills\n      .slice(0, priority === 'quick' ? 3 : 10)\n      .map((skill) => ({\n        skill,\n        resources: this.skillResources[skill] || [],\n        estimatedWeeks: Math.ceil(\n          (this.skillResources[skill]?.[0]?.hours || 30) / 10\n        ),\n      }));\n\n    return {\n      totalEstimatedWeeks: path.reduce((sum, p) => sum + p.estimatedWeeks, 0),\n      skills: path,\n      tip: `Focus on ${gaps.missingSkills[0]} first - it's most critical`,\n    };\n  }\n}\n\n// Usage\nconst generator = new LearningPathGenerator();\nconst path = generator.generateLearningPath(\n  ['Python', 'Git'],\n  ['Python', 'Docker', 'Kubernetes', 'AWS'],\n  'comprehensive'\n);\n\nconsole.log(`Learn these skills in ${path.totalEstimatedWeeks} weeks:`);\npath.skills.forEach((s) => {\n  console.log(`- ${s.skill} (${s.estimatedWeeks} weeks)`);\n  s.resources.forEach((r) => {\n    console.log(`  → ${r.name} (${r.source}, ${r.hours}h)`);\n  });\n});\n``\n\n---\n\n### Pattern 3: Resume Optimization Simulator\n\n**Use case:** Show candidate what score they'd get with improvements\n\n``typescript\nimport { MLModulesManager } from '@/services/agents/ml-modules';\n\nclass ResumeOptimizationSimulator {\n  private mlManager: MLModulesManager;\n\n  constructor() {\n    this.mlManager = new MLModulesManager();\n  }\n\n  async simulateImprovement(\n    resume: ResumeData,\n    jobInput: any,\n    improvementScenario: {\n      addSkills?: string[];      // Skills to add\n      removeSkills?: string[];   // Skills to remove\n      updateSummary?: string;    // New summary text\n    }\n  ) {\n    // Baseline analysis\n    const baseline = await this.mlManager.analyzeMatch(resume, jobInput);\n\n    // Simulate improvements\n    const simulatedResume = { ...resume };\n\n    // Add skills\n    if (improvementScenario.addSkills) {\n      const currentSkills = simulatedResume.sections?.skills?.items || [];\n      for (const skill of improvementScenario.addSkills) {\n        currentSkills.push({ name: skill });\n      }\n    }\n\n    // Update summary\n    if (improvementScenario.updateSummary && simulatedResume.summary) {\n      simulatedResume.summary.content = improvementScenario.updateSummary;\n    }\n\n    // Re-analyze\n    const improved = await this.mlManager.analyzeMatch(simulatedResume, jobInput);\n\n    // Calculate delta\n    const delta = improved.scoring.finalScore - baseline.scoring.finalScore;\n\n    return {\n      baseline: baseline.scoring.finalScore,\n      improved: improved.scoring.finalScore,\n      delta: Math.round(delta * 10) / 10,\n      newTier: improved.scoring.matchTier,\n      simulation: {\n        changedSkills: improvementScenario.addSkills?.length || 0,\n        recommendation:\n          delta > 10\n            ? \"Great! These changes would significantly improve your match.\"\n            : delta > 0\n              ? \"These changes would help, but more work needed.\"\n              : \"These changes won't help much. Try different approach.\",\n      },\n    };\n  }\n}\n\n// Usage\nconst simulator = new ResumeOptimizationSimulator();\nconst result = await simulator.simulateImprovement(\n  resume,\n  jobInput,\n  {\n    addSkills: ['Docker', 'Kubernetes'],\n    updateSummary: '10+ years backend development with focus on scalability...',\n  }\n);\n\nconsole.log(`\nIf you make these changes:\n- Current score: ${result.baseline}\n- New score: ${result.improved} (${result.newTier})\n- Improvement: +${result.delta} points\n${result.simulation.recommendation}\n`);\n``\n\n---\n\n## Error Handling\n\n### Safe Wrapper Pattern\n\n`typescript\nimport { MLModulesManager } from '@/services/agents/ml-modules';\n\nclass SafeMLAnalysis {\n  private mlManager: MLModulesManager;\n\n  constructor() {\n    this.mlManager = new MLModulesManager();\n  }\n\n  async analyzeWithFallback(\n    resume: ResumeData,\n    jobInput: any\n  ): Promise<any> {\n    try {\n      // Try full analysis with LLM\n      return await this.mlManager.analyzeMatch(resume, jobInput, true);\n    } catch (fullError) {\n      try {\n        // Fallback: Try without LLM\n        console.warn('LLM analysis failed, using rule-based recommendations');\n        return await this.mlManager.analyzeMatch(resume, jobInput, false);\n      } catch (fallbackError) {\n        // Last resort: Return minimal result\n        console.error('ML analysis completely failed:', fallbackError);\n        return this.getMinimalResult(resume, jobInput);\n      }\n    }\n  }\n\n  private getMinimalResult(resume: ResumeData, jobInput: any) {\n    // Provide basic information when everything fails\n    return {\n      scoring: {\n        finalScore: 50,\n        matchTier: 'unknown',\n        reasoning: 'Unable to analyze; please try again later',\n      },\n      error: 'Analysis temporarily unavailable',\n      fallback: true,\n    };\n  }\n}\n`\n\n---\n\n## Testing Examples\n\n`typescript\nimport { describe, it, expect } from 'vitest';\nimport { analyzeSkillGap } from '@/services/agents/ml-modules';\n\ndescribe('ML Modules', () => {\n  it('should identify skill gaps accurately', () => {\n    const gap = analyzeSkillGap(\n      ['Python', 'Docker', 'K8s', 'AWS'],\n      ['Python', 'Django', 'AWS']\n    );\n\n    expect(gap.matchedSkills).toEqual(['Python', 'AWS']);\n    expect(gap.missingSkills).toEqual(['Docker', 'K8s']);\n    expect(gap.bonusSkills).toEqual(['Django']);\n    expect(gap.skillCoverage).toBe(50); // 2 out of 4\n    expect(gap.gapPriority).toBe('high');\n  });\n\n  it('should handle case-insensitive skill matching', () => {\n    const gap = analyzeSkillGap(['PYTHON'], ['python']);\n    expect(gap.matchedSkills.length).toBe(1);\n  });\n\n  it('should normalize skill name variations', () => {\n    const gap = analyzeSkillGap(['Node.js', 'C#'], ['nodejs', 'csharp']);\n    expect(gap.matchedSkills.length).toBe(2);\n  });\n});\n`\n\n---\n\n## Performance Optimization\n\n### Caching Embeddings\n\n``typescript\nclass CachedMLAnalysis {\n  private cache = new Map<string, any>();\n\n  private getCacheKey(resume: ResumeData, jobInput: any): string {\n    return `${resume.basics?.name}:${jobInput.title}`;\n  }\n\n  async analyzeMatch(resume: ResumeData, jobInput: any) {\n    const key = this.getCacheKey(resume, jobInput);\n    if (this.cache.has(key)) {\n      return this.cache.get(key);\n    }\n\n    const result = await this.mlManager.analyzeMatch(resume, jobInput);\n    this.cache.set(key, result);\n    return result;\n  }\n}\n``\n\n---\n\n## Summary\n\nThese patterns demonstrate:\n- ✅ Quick screening of large candidate pools\n- ✅ Detailed candidate feedback generation\n- ✅ HRAgent integration strategies\n- ✅ Advanced use cases (batch processing, learning paths, simulation)\n- ✅ Error handling and fallback logic\n- ✅ Performance optimization techniques\n"}
