# ML Modules - Quick Reference Card\n\n## Import Statements\n\n`typescript\n// Quick one-liner\nimport { quickAnalyze } from '@/services/agents/ml-modules';\n\n// Full manager\nimport { MLModulesManager } from '@/services/agents/ml-modules';\n\n// Individual modules\nimport { analyzeSkillGap, generateSkillGapSuggestions } from '@/services/agents/ml-modules';\nimport { calculateSemanticSimilarity, extractResumeText } from '@/services/agents/ml-modules';\nimport { calculateScoreBreakdown, MATCH_TIER_THRESHOLDS } from '@/services/agents/ml-modules';\nimport { generateImprovements } from '@/services/agents/ml-modules';\n`\n\n---\n\n## Function Signatures\n\n### Semantic Matching\n\n`typescript\n// Calculate semantic similarity\nconst result = await calculateSemanticSimilarity(\n  resumeData: ResumeData,\n  jobInput: any,\n  config?: SemanticMatchConfig\n): Promise<SemanticMatchResult>;\n\n// Returns: { semanticSimilarity: 0-1, semanticScore: 0-100, matchLogic, reasoning }\n`\n\n### Skill Gap Analysis\n\n`typescript\n// Analyze skill gaps\nconst gap = analyzeSkillGap(\n  jobSkills: string[],\n  resumeSkills: string[]\n): SkillGapAnalysis;\n\n// Returns: { matchedSkills, missingSkills, bonusSkills, skillCoverage%, gapPriority }\n\n// Generate gap suggestions\nconst suggestions = generateSkillGapSuggestions(\n  gap: SkillGapAnalysis,\n  jobTitle: string\n): string[];\n`\n\n### Scoring\n\n`typescript\n// Calculate score breakdown\nconst breakdown = calculateScoreBreakdown(\n  semanticScore: number,        // 0-100\n  skillGap: SkillGapAnalysis,\n  config?: SemanticMatchConfig\n): ScoreBreakdown;\n\n// Returns: { finalScore, matchTier, semanticComponent, skillComponent, reasoning }\n\n// Generate improvement plan\nconst plan = generateImprovementPlan(\n  breakdown: ScoreBreakdown,\n  skillGap: SkillGapAnalysis\n): string[];\n`\n\n### Improvements\n\n`typescript\n// Generate recommendations\nconst improvements = await generateImprovements(\n  resume: ResumeData,\n  jobInput: any,\n  skillGap: SkillGapAnalysis,\n  scoreBreakdown: ScoreBreakdown,\n  useLLM?: boolean  // default false\n): Promise<ImprovementResult>;\n\n// Returns: { recommendations[], summary, estimatedScoreIncrease, priorities, usedLLM }\n`\n\n### Manager\n\n`typescript\n// Complete analysis\nconst analysis = await manager.analyzeMatch(\n  resume: ResumeData,\n  jobInput: any,\n  useLLM?: boolean\n): Promise<CompleteMLAnalysis>;\n\n// Or quick version\nconst analysis = await quickAnalyze(resume, jobInput, useLLM?);\n`\n\n---\n\n## Common Usage Patterns\n\n### Pattern 1: Quick Score Only\n\n`typescript\nconst result = await quickAnalyze(resume, jobInput);\nconsole.log(result.scoring.finalScore);   // 78.5\n`\n\n### Pattern 2: Full Analysis with Recommendations\n\n`typescript\nconst manager = new MLModulesManager();\nconst analysis = await manager.analyzeMatch(resume, jobInput, true);  // true = use LLM\nconsole.log(analysis.improvements.recommendations);\n`\n\n### Pattern 3: Custom Weights\n\n`typescript\nconst manager = new MLModulesManager({\n  semanticWeight: 0.6,\n  skillWeight: 0.4\n});\nconst analysis = await manager.analyzeMatch(resume, jobInput);\n`\n\n### Pattern 4: Batch Screening\n\n`typescript\nfor (const resume of resumes) {\n  const result = await quickAnalyze(resume, jobInput);\n  if (result.scoring.finalScore >= 75) {\n    qualified.push(resume);\n  }\n}\n`\n\n---\n\n## Output Examples\n\n### Skill Gap Output\n\n`typescript\n{\n  matchedSkills: ['Python', 'AWS'],\n  missingSkills: ['Docker', 'Kubernetes'],\n  bonusSkills: ['Django', 'PostgreSQL'],\n  skillCoverage: 50,          // percentage\n  gapPriority: 'high'         // critical | high | medium | low\n}\n`\n\n### Score Breakdown Output\n\n`typescript\n{\n  finalScore: 73.4,\n  matchTier: 'qualified',     // excellent | good | qualified | unqualified\n  semanticComponent: {\n    score: 78,\n    weight: 0.7,\n    contribution: 54.6\n  },\n  skillComponent: {\n    score: 50,\n    weight: 0.3,\n    contribution: 15.0\n  },\n  reasoning: 'Your background has basic alignment...'\n}\n`\n\n### Improvement Recommendation Output\n\n`typescript\n{\n  category: 'skill_gap',                           // or semantic_alignment, etc\n  priority: 'critical',                           // critical | high | medium | low\n  suggestion: 'Add Docker and Kubernetes',\n  reasoning: 'These are required skills...',\n  implementation: '1. Take online course...\\n2. Complete project...',\n  impact: '+10-15 points on skill coverage',\n  examples: ['Example 1', 'Example 2']\n}\n`\n\n---\n\n## Key Thresholds\n\n`typescript\n// Match tiers\nMATCH_TIER_THRESHOLDS = {\n  excellent: { min: 90, max: 100 },     // Highly recommended\n  good: { min: 80, max: 89 },           // Worth considering\n  qualified: { min: 60, max: 79 },      // Basic alignment\n  unqualified: { min: 0, max: 59 }      // Limited alignment\n}\n\n// Skill gap priorities\n// <30%: critical\n// 30-60%: high\n// 60-85%: medium\n// >85%: low\n`\n\n---\n\n## Configuration Defaults\n\n`typescript\n{\n  semanticWeight: 0.7,        // 70% of score\n  skillWeight: 0.3,           // 30% of score\n  embeddingDim: 128,          // Lightweight embeddings\n  useTransformer: false       // Lightweight by default\n}\n`\n\n---\n\n## Performance Quick Reference\n\n| Operation | Time |\n|-----------|------|\n| Semantic match (lightweight) | <10ms |\n| Semantic match (transformer) | 50-100ms |\n| Skill gap analysis | <1ms |\n| Scoring calculation | <1ms |\n| Rule-based recommendations | ~50ms |\n| LLM recommendations | 2-5s |\n| **Total lightweight** | ~60ms |\n| **Total with LLM** | ~5-6s |\n\n---\n\n## Dependencies\n\n`bash\n# Required\npnpm add ai@^3.0.0\n\n# Optional\npnpm add @xenova/transformers@^2.10.0\n`\n\n---\n\n## Error Handling\n\n`typescript\ntry {\n  const analysis = await quickAnalyze(resume, jobInput);\n} catch (err) {\n  // Falls back automatically:\n  // 1. LLM fails → Use rules\n  // 2. Transformers fail → Use lightweight\n  // 3. Everything fails → Return minimal result\n}\n`\n\n---\n\n## Troubleshooting\n\n| Issue | Solution |\n|-------|----------|\n| Low semantic scores | Ensure resume has detailed descriptions, skills list |\n| LLM not generating | Check API keys in environment variables |\n| Skill matches missing | Skills might be formatted differently; check normalization |\n| Slow performance | Disable LLM; use lightweight embeddings |\n| Import errors | Ensure `ml-modules` folder is in correct path |\n\n---\n\n## Type Definitions\n\n`typescript\ninterface SemanticMatchResult {\n  semanticSimilarity: number;     // 0-1\n  semanticScore: number;          // 0-100\n  matchLogic: string;\n  reasoning: string;\n}\n\ninterface SkillGapAnalysis {\n  matchedSkills: string[];\n  missingSkills: string[];\n  bonusSkills: string[];\n  skillCoverage: number;          // 0-100\n  gapPriority: 'critical' | 'high' | 'medium' | 'low';\n}\n\ninterface ScoreBreakdown {\n  finalScore: number;             // 0-100\n  matchTier: 'excellent' | 'good' | 'qualified' | 'unqualified';\n  semanticComponent: { score, weight, contribution };\n  skillComponent: { score, weight, contribution };\n  reasoning: string;\n}\n\ninterface ImprovementRecommendation {\n  category: 'skill_gap' | 'semantic_alignment' | 'achievement_emphasis' | 'formatting' | 'general';\n  priority: 'critical' | 'high' | 'medium' | 'low';\n  suggestion: string;\n  reasoning: string;\n  implementation: string;\n  impact: string;\n  examples?: string[];\n}\n\ninterface CompleteMLAnalysis {\n  semantic: SemanticMatchResult;\n  skillGap: SkillGapAnalysis;\n  scoring: ScoreBreakdown;\n  matchScoreBreakdown: { semanticComponent, skillComponent, finalScore, formula };\n  improvements: { recommendations, summary, estimatedScoreIncrease, priorities, usedLLM };\n  metadata: { analysisTimestamp, analysisVersion };\n}\n`\n\n---\n\n## One-Liners\n\n`typescript\n// Get score\nconst score = (await quickAnalyze(resume, job)).scoring.finalScore;\n\n// Get gaps\nconst gaps = (await quickAnalyze(resume, job)).skillGap.missingSkills;\n\n// Get tier\nconst tier = (await quickAnalyze(resume, job)).scoring.matchTier;\n\n// Get recommendations\nconst recs = (await quickAnalyze(resume, job)).improvements.recommendations;\n`\n\n---\n\n## Integration Checklist\n\n- [ ] Installed `ai` package\n- [ ] Imported MLModulesManager\n- [ ] Set up environment variables (if using LLM)\n- [ ] Created manager instance\n- [ ] Called `analyzeMatch()` or `quickAnalyze()`\n- [ ] Processed results\n- [ ] Tested with sample resumes\n- [ ] Handled errors gracefully\n- [ ] Configured weights if needed\n- [ ] Deployed to production\n\n---\n\n## Documentation Quick Links\n\n- **Setup & Installation**: DEPENDENCIES.md\n- **Detailed Usage**: IMPLEMENTATION_GUIDE.md\n- **Real Examples**: USAGE_EXAMPLES.md\n- **Module Details**: README.md\n- **This Quick Ref**: QUICK_REFERENCE.md ← You are here\n\n---\n\n## Support\n\nFor detailed documentation, see IMPLEMENTATION_GUIDE.md \nFor examples, see USAGE_EXAMPLES.md \nFor setup help, see DEPENDENCIES.md\n"}
