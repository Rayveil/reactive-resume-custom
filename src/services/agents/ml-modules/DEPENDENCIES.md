/**\n _ Dependencies & Requirements for ML Modules\n _ \n _ This file documents all required and optional dependencies\n _ for the ML modules suite.\n \*/\n\n// ============================================================================\n// REQUIRED DEPENDENCIES\n// ============================================================================\n\n/**\n _ ai - LLM calling library\n _ Version: >= 3.0.0\n _ Used by: improvement-generator.ts (LLM-based recommendations)\n _ \n _ Installation:\n _ pnpm add ai\n _ \n _ Configuration:\n _ Set API_KEY environment variables for your LLM provider\n _ Examples: OPENAI_API_KEY, ANTHROPIC_API_KEY, etc.\n _/\nexport const REQUIRED_AI = \"ai@^3.0.0\";\n\n// ============================================================================\n// OPTIONAL DEPENDENCIES\n// ============================================================================\n\n/\*\*\n _ sentence-transformers (Python) - For advanced semantic embeddings\n _ Version: >= 2.2.0\n _ Used by: semantic-matcher.ts (can replace lightweight embeddings)\n _ \n _ Note: This is a Python library; for TypeScript use sentence-transformers\n _ JavaScript bindings or ONNX Runtime\n _ \n _ JavaScript alternatives:\n _ - @xenova/transformers - WASM-based, browser/Node.js compatible\n _ - onnxruntime-web/node - For running ONNX models\n _ \n _ Installation (WASM-based):\n _ pnpm add @xenova/transformers\n _ \n _ When to use:\n _ - If you want better semantic similarity (vs lightweight hash-based embeddings)\n _ - If analyzing large batches of resumes (performance trade-off)\n _ - If working in resource-constrained environment (use lightweight fallback)\n _/\nexport const OPTIONAL_TRANSFORMERS = \"@xenova/transformers@^2.10.0\";\n\n/**\n _ sklearn or similar - For advanced metrics\n _ Note: TypeScript has limited ML library ecosystem\n _ \n _ Alternatives:\n _ - simple-statistics: Lightweight statistical functions\n _ - ml-distance: Distance metrics\n _ \n _ Installation:\n _ pnpm add simple-statistics ml-distance\n _/\nexport const OPTIONAL_METRICS = {\n \"simple-statistics\": \"^9.0.0\",\n \"ml-distance\": \"^2.0.0\",\n};\n\n// ============================================================================\n// PROJECT DEPENDENCIES (Already in your project)\n// ============================================================================\n\n/**\n _ From your existing reactive-resume project:\n _ - ResumeData schema (src/schema/resume/data.ts)\n _ - shared-memory utilities (src/services/agents/shared-memory.ts)\n _ - textToEmbedding() - lightweight embeddings (128-dim hash-based)\n _ - cosineSimilarity() - vector similarity\n _ - resumeDataToText() - text extraction\n _/\nexport const PROJECT_DEPENDENCIES = [\n \"src/schema/resume/data.ts (ResumeData type)\",\n \"src/services/agents/shared-memory.ts (embedding utilities)\",\n];\n\n// ============================================================================\n// MINIMUM VIABLE SETUP\n// ============================================================================\n\n/\*\*\n _ Required dependencies for basic functionality:\n _ pnpm add ai\n _ \n _ This provides:\n _ - Semantic matching (via lightweight embeddings from shared-memory)\n _ - Skill gap analysis (no external deps, pure set operations)\n _ - Scoring engine (no external deps, pure calculations)\n _ - Improvement generator with rule-based fallback (or LLM if configured)\n _/\n\n// ============================================================================\n// FULL-FEATURED SETUP\n// ============================================================================\n\n/**\n _ For best performance and quality:\n _ pnpm add ai @xenova/transformers simple-statistics\n _ \n _ This provides:\n _ - Advanced semantic embeddings (SentenceTransformer via WASM)\n _ - Statistical utilities for better analysis\n _ - LLM-powered recommendations\n _ \n _ Performance impact:\n _ - First load: ~500ms (downloading WASM model)\n _ - Per match: 50-100ms (vs <10ms for lightweight)\n _ - Accuracy improvement: ~20-30% better semantic matching\n \*/\n\n// ============================================================================\n// FALLBACK LOGIC\n// ============================================================================\n\n/**\n _ Graceful degradation strategy (inspired by Placify):\n _ \n _ 1. Try to use advanced embeddings (SentenceTransformer)\n _ 2. Fall back to lightweight embeddings (hash-based, no deps)\n _ 3. For scoring: pure calculation, always works\n _ 4. For improvements: Try LLM → Fall back to rule-based\n _ \n _ This ensures the system works even with minimal dependencies.\n _/\n\n// ============================================================================\n// DEVELOPMENT DEPENDENCIES\n// ============================================================================\n\n/\*\*\n _ For testing and development:\n _ pnpm add -D @types/node typescript jest @testing-library/jest-dom\n _/\n\n// ============================================================================\n// ENVIRONMENT VARIABLES\n// ============================================================================\n\n/**\n _ Required environment variables:\n _ \n _ For LLM support:\n _ OPENAI_API_KEY=sk-... (if using OpenAI)\n _ ANTHROPIC_API_KEY=... (if using Claude)\n _ GEMINI_API_KEY=... (if using Google Gemini)\n _ \n _ Optional:\n _ ML_USE_TRANSFORMERS=true/false (enable advanced embeddings)\n _ ML_USE_LLM=true/false (enable LLM-based recommendations)\n _ ML_DEBUG=true/false (verbose logging)\n _/\n\n// ============================================================================\n// VERSION COMPATIBILITY\n// ============================================================================\n\n/**\n _ Module versions tested with:\n _ - Node.js: >= 16.0.0\n _ - TypeScript: >= 4.5.0\n _ - React: 19 (from your project)\n _ - Zod: Latest (schema validation)\n _/\n\n// ============================================================================\n// INSTALLATION GUIDE\n// ============================================================================\n\n/**\n _ Step 1: Install required dependencies\n _ pnpm add ai\n _ \n _ Step 2: (Optional) Install for better semantic matching\n _ pnpm add @xenova/transformers\n _ \n _ Step 3: Set up environment variables\n _ .env:\n _ OPENAI_API_KEY=sk-...\n _ ML_USE_TRANSFORMERS=true\n _ \n _ Step 4: Import and use\n _ import { MLModulesManager, quickAnalyze } from '@/services/agents/ml-modules';\n _ \n _ const manager = new MLModulesManager();\n _ const analysis = await manager.analyzeMatch(resume, jobInput, useLLM=true);\n \*/\n\n// ============================================================================\n// PACKAGE.JSON SNIPPET\n// ============================================================================\n\n/**\n _ Add to package.json dependencies:\n _ \n _ {\n _ \"dependencies\": {\n _ \"ai\": \"^3.0.0\",\n _ \"@xenova/transformers\": \"^2.10.0\" // optional, for better embeddings\n _ },\n _ \"devDependencies\": {\n _ \"@types/node\": \"latest\"\n _ }\n _ }\n _/\n\nexport const PACKAGE_JSON_SNIPPET = `\n{\n  \"dependencies\": {\n    \"ai\": \"^3.0.0\"\n  },\n  \"optionalDependencies\": {\n    \"@xenova/transformers\": \"^2.10.0\",\n    \"simple-statistics\": \"^9.0.0\"\n  }\n}\n`;\n"}
